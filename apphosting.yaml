scripts:
  buildCommand: npm run build
  runCommand: npm run start

runConfig:
  minInstances: 0
  maxInstances: 2
  cpu: 1
  memoryMiB: 512
  concurrency: 10

outputFiles:
  serverApp:
    include: [dist]
